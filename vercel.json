{
    "experimentalServices": {
        "api": {
            "root": "apps/api",
            "entrypoint": "src/main.ts",
            "routePrefix": "/api",
            "buildCommand": "npm install && npm run build"
        },
        "web": {
            "root": "apps/web",
            "routePrefix": "/",
            "framework": "vite",
            "buildCommand": "npm install && npm run build"
        }
    }
}

